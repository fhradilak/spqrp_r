% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessing.R
\name{plate_correct_residuals_by_protein}
\alias{plate_correct_residuals_by_protein}
\title{Regress intensity on plate and replace it with OLS residuals}
\usage{
plate_correct_residuals_by_protein(
  group_data,
  individual = PATIENT,
  sample = SAMPLE,
  impute = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{group_data}{Long-format intensity data frame.}

\item{individual}{Patient identifier column (default `"Patient_ID"`).}

\item{sample}{Sample identifier column (default `"Sample_ID"`).}

\item{impute}{If `TRUE`, impute missing intensities by patient/protein
median before regression; otherwise drop NA rows.}

\item{verbose}{If `TRUE`, also build before/after boxplots.}
}
\value{
Tibble with corrected `Intensity`. Attribute `"plot"` carries
  the diagnostic ggplot when `verbose = TRUE`.
}
\description{
Identifies a `plate` (or `Plate`) column, encodes it as integers, fits
`lm(Intensity ~ plate)`, and replaces `Intensity` with the model's
residuals. If no plate column is present, returns the input unchanged
with a message.
}
\examples{
\donttest{
df <- spqrp_example_data("input_cohort_df")
df$plate <- rep(c("A", "B"), length.out = nrow(df))
corrected <- plate_correct_residuals_by_protein(df)
head(corrected)
}
}
