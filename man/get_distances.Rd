% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{get_distances}
\alias{get_distances}
\title{Pairwise distance matrix on a long-format intensity table}
\usage{
get_distances(
  df_dist,
  metric = "correlation",
  intensity = "Intensity",
  fractional_p = 0.5,
  index = SAMPLE
)
}
\arguments{
\item{df_dist}{Long-format data frame with `Sample_ID`, `Protein`, and
the `intensity` column.}

\item{metric}{One of `"correlation"`, `"euclidean"`, `"manhattan"`,
`"minkowski"`, or `"fractional"` (Minkowski with `p = fractional_p`).}

\item{intensity}{Name of the intensity column. Defaults to `"Intensity"`.}

\item{fractional_p}{Exponent for the fractional / Minkowski metric.}

\item{index}{Column used as the sample identifier. Defaults to
`"Sample_ID"`.}
}
\value{
A list with `distance_matrix` (numeric matrix with row/col names
  set to sample IDs) and `df_pivot` (the wide tibble used to compute it).
}
\description{
Pivots the long-format `df_dist` to a (sample x protein) wide matrix
(missing values filled with 0) and computes pairwise distances using
the requested metric.
}
\keyword{internal}
