% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protein_selection.R
\name{train_with_normalise}
\alias{train_with_normalise}
\title{End-to-end ranking pipeline: filter, normalise, optionally plate-correct, train RF}
\usage{
train_with_normalise(
  df,
  threshold = 0.7,
  test_size = 0.3,
  plate_corrected = TRUE,
  individual = PATIENT,
  sample = SAMPLE,
  compute_euclid = FALSE,
  method = "F1",
  outlier_removal = TRUE,
  train_individuals = NULL,
  test_individuals = NULL,
  sample_decision_curve = FALSE,
  classifier_backend = c("randomForest", "ranger", "themis_smote"),
  importance_method = "impurity",
  plot_per_sample = FALSE,
  absolute = FALSE,
  quiet = TRUE
)
}
\arguments{
\item{df}{Long-format cohort data frame.}

\item{threshold}{Occurrence-filter threshold.}

\item{test_size}{Patient-level test fraction.}

\item{plate_corrected}{If `TRUE`, run plate-effect residualisation.}

\item{individual}{Patient column.}

\item{sample}{Sample column.}

\item{compute_euclid}{Add NaN-aware Euclidean distance feature.}

\item{method}{Threshold-selection strategy.}

\item{outlier_removal}{Run [by_isolation_forest()] on each split.}

\item{train_individuals, test_individuals}{Explicit split overrides.}

\item{sample_decision_curve}{Draw ROC/PR curves.}

\item{classifier_backend}{`"randomForest"` (default -- closest behaviour
to Python's `imblearn.BalancedRandomForestClassifier`), `"ranger"`
(faster), or `"themis_smote"`. The default was changed from
`"ranger"` to `"randomForest"` to bring R rankings closer to the
Python port. See
<https://github.com/fhradilak/spqrp_r/blob/main/articles/numerical-divergence.md>.}

\item{importance_method}{Unused placeholder (kept for API parity).}

\item{plot_per_sample}{Per-sample probability plots.}

\item{absolute}{Use absolute pairwise differences.}

\item{quiet}{If `TRUE` (default), suppress informational status messages
(train/test split listing, "Proteins only in test set", outliers
removed, fold headers, per-fold metrics, top-importance list, and
per-misclassified-pair prints) and skip auto-rendering of the ROC
/ PR / probability plots. Set `FALSE` to print everything.
Warnings about genuine data issues are emitted regardless.}
}
\value{
`spqrp_train` S3 object (a named list with classifier, pair
  indices, feature importances, misclassified pairs).
}
\description{
Mirrors `protein_selection.train_with_normalise` from the Python package
but exposes `classifier_backend` so users can compare three RF variants
(`"ranger"`, `"randomForest"`, `"themis_smote"`). See
<https://github.com/fhradilak/spqrp_r/blob/main/articles/numerical-divergence.md>
for the tradeoffs.
}
\examples{
\donttest{
df <- spqrp_example_data("input_cohort_df")
res <- train_with_normalise(df, plate_corrected = FALSE,
                             outlier_removal = FALSE)
retrieve_ranking(res)
}
}
