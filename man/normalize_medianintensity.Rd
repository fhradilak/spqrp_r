% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessing.R
\name{normalize_medianintensity}
\alias{normalize_medianintensity}
\title{Per-sample median normalisation (log-space subtraction)}
\usage{
normalize_medianintensity(
  dataset,
  string_of_pool = "",
  revert_log = FALSE,
  sample = SAMPLE,
  plot = TRUE
)
}
\arguments{
\item{dataset}{Long-format intensity data frame.}

\item{string_of_pool}{If non-empty, samples whose ID contains this
substring are excluded from normalization (kept out of the
post-normalization data).}

\item{revert_log}{If `TRUE`, run [revert_log_transform()] first.}

\item{sample}{Column to group by (defaults to `"Sample_ID"`).}

\item{plot}{If `TRUE`, attach a before/after boxplot.}
}
\value{
List with `data` and (optionally) `plot`.
}
\description{
Subtracts each sample's median intensity from its intensities, then
re-centers on the dataset's overall median. By default `df` is assumed
to already be in log space. If `revert_log = TRUE`, the function reverts
the log transform first, then divides by the per-sample median ratio.
}
\details{
Returns a list with `data` (the normalized tibble) and `plot` (a
ggplot showing before/after boxplots).
}
\examples{
df <- spqrp_example_data("input_cohort_df")
norm <- normalize_medianintensity(log_transform(df), plot = FALSE)
head(norm$data)
}
