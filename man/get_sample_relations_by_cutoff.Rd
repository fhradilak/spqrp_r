% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{get_sample_relations_by_cutoff}
\alias{get_sample_relations_by_cutoff}
\title{Split pairwise distances into belonging / not-belonging by cutoff}
\usage{
get_sample_relations_by_cutoff(
  distance_matrix,
  cutoff,
  sample_patient_mapping,
  sample_order,
  quiet = TRUE
)
}
\arguments{
\item{distance_matrix}{Symmetric numeric matrix.}

\item{cutoff}{Numeric cutoff.}

\item{sample_patient_mapping}{Named character vector (names = `Sample_ID`,
values = `Patient_ID`).}

\item{sample_order}{Row/col order of `distance_matrix` as a character
vector of sample IDs.}

\item{quiet}{If `TRUE` (default), suppress informational status messages.
Set `FALSE` to print progress and per-call summaries (sample counts,
chosen cutoff, etc.). Warnings about genuine data issues -- e.g.
samples dropped from the analysis -- are emitted regardless.}
}
\value{
List with `belonging` and `not_belonging` tibbles.
}
\description{
Pairs with `distance <= cutoff` go into `belonging`, others into
`not_belonging`. Patient IDs are looked up via `sample_patient_mapping`.
}
\keyword{internal}
