% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_distribution_with_highlights}
\alias{plot_distribution_with_highlights}
\title{Histogram with FN/FP/percentile overlays and a legend}
\usage{
plot_distribution_with_highlights(
  distances,
  fn_distances,
  fp_distances,
  percentiles = c(1, 2, 5, 10),
  name = "",
  print = TRUE,
  figsize = c(8, 5),
  dpi = 150L,
  save_path = NULL,
  quiet = TRUE
)
}
\arguments{
\item{distances}{Numeric vector of all pairwise distances.}

\item{fn_distances}{Distances of false-negative pairs. Pass `numeric(0)` or
`NULL` to omit the FN legend entry.}

\item{fp_distances}{Distances of false-positive pairs. Pass `numeric(0)` or
`NULL` to omit the FP legend entry.}

\item{percentiles}{Percentiles (0-100) to draw as vertical lines. Each
percentile becomes its own legend entry.}

\item{name}{Title suffix appended to "Distribution of Pairwise Distances".
Set this to a cohort label so saved plots are self-documenting.}

\item{print}{If `TRUE`, print the plot.}

\item{figsize}{Numeric vector of length 2: width and height in inches for
`ggsave` output. Default `c(8, 5)` matches Python's `plt.figure(figsize=(8, 5))`.}

\item{dpi}{Resolution (dots per inch) for the saved file. Default `150`.}

\item{save_path}{Where to save a high-resolution PNG/SVG/PDF render.
Accepts:
* `NULL` (default) -- don't save; only return the ggplot object. The
  function prints a hint about how to save.
* a character path (e.g. `"distances.png"`) -- save there via
  `ggsave()`. Extension chooses the format.}

\item{quiet}{If `TRUE` (default), suppress the informational
`save_path` hints. Warnings about genuine data issues are emitted
regardless.}
}
\value{
Invisible ggplot.
}
\description{
Mirrors Python's `plot_distribution_with_highlights` (helpers.py): a grey
histogram of all pairwise distances, overlaid with vertical lines for
false-negative pairs (blue), false-positive pairs (orange), and percentile
cutoffs (magenta). The legend names each category, matching the Python
figure key.
}
\keyword{internal}
