% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{cluster_samples_iteratively}
\alias{cluster_samples_iteratively}
\title{Iterative-clustering primitive: build a kNN graph + 2D coords}
\usage{
cluster_samples_iteratively(
  result,
  df,
  method = "UMAP",
  random_state = 42L,
  n_neighbors = 1L,
  max_component_size = NULL,
  n_umap_neighbors = 15L,
  precomputed_graph = NULL,
  mds_backend = c("cmdscale", "smacof"),
  quiet = TRUE
)
}
\arguments{
\item{result}{Output of [calculate_pairwise_distances()] (uses
`distance_matrix`).}

\item{df}{Long-format cohort data frame.}

\item{method}{`"UMAP"`, `"PCA"`, or `"MDS"`.}

\item{random_state}{Seed for the dimensionality reduction.}

\item{n_neighbors}{Number of nearest-neighbour edges per sample.}

\item{max_component_size}{If non-NULL, iteratively split clusters
above this size.}

\item{n_umap_neighbors}{UMAP's `n_neighbors` parameter.}

\item{precomputed_graph}{Optional precomputed igraph object.}

\item{mds_backend}{`"cmdscale"` (default) or `"smacof"` (Suggests).}

\item{quiet}{If `TRUE` (default), suppress informational status messages.
Set `FALSE` to print progress and per-call summaries (sample counts,
chosen cutoff, etc.). Warnings about genuine data issues -- e.g.
samples dropped from the analysis -- are emitted regardless.}
}
\value{
List with `G` (igraph) and `coords_2d` (matrix).
}
\description{
Builds a 2D embedding via PCA/UMAP/MDS, connects each sample to its
`n_neighbors` nearest neighbours in distance space, and optionally
splits components larger than `max_component_size` by repeatedly
removing the largest-weight edge.
}
\keyword{internal}
